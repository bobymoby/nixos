{ ... }:

{
  bobymoby = {
    wayland = {
      enable = true;
      hyprland = {
        enable = true;
        usePcConfig = true;
      };
      waybar.usePcConfig = true;
    };

    shell.zsh.enable = true;
    # gaming.enable = true;
    jetbrains.enable = false;
  };
}
