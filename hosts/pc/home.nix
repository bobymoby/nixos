{ ... }:

{
  bobymoby = {
    wayland = {
      enable = true;
      hyprland = {
        enable = true;
        useLaptopConfig = true;
      };
    };
    shell.zsh.enable = true;
    # gaming.enable = true;
  };
}
